Flow adaptive.login
     Basepath ""
credentials = RRF "login.ftlh" 
riskHigh = Call valid#validateCredentials credentials
When riskHigh is true
     otp = RRF "otp.ftlh" 
     otpValid = Call valid#validateOtp otp
     When otpValid is false
     Otherwise
          Finish __________
Otherwise
     Finish __________